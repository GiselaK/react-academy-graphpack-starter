type Query {
  articles(searchText: String): [Article!]!
  users(limit: Int): [User!]!
  article(id: String!): Article
  user(id: String!): User
}

type Article {
  id: ID!
  title: String
  body: String
}

type User {
  id: ID!
  name: String
  email: String
  isEmployed: Boolean
  age: Int
}